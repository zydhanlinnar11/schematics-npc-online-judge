version: '3.7'
services:
  db:
    container_name: dmoj_mysql
    image: mariadb
    restart: always
    volumes:
      - ./database/:/var/lib/mysql/
    env_file: [environment/mysql.env, environment/mysql-admin.env]
    networks: [db]
  redis:
    container_name: dmoj_redis
    image: redis:alpine
    restart: always
    networks: [site]
  base:
    build:
      context: .
      dockerfile: ./base/Dockerfile
    image: zydhanlinnar11/dmoj-base
    network_mode: none
  site:
    build:
      context: .
      dockerfile: ./site/Dockerfile
    image: zydhanlinnar11/dmoj-site
    restart: unless-stopped
    volumes:
      - ./logs/:/logs/
      - ./media/:/media/
      - ./problems/:/problems/
      - ./repo/:/site/
      - assets:/assets/
      - pdf:/pdf/
    working_dir: /site/
    env_file: [environment/mysql.env, environment/site.env]
    networks: [site, nginx, db]
    depends_on: [base, db, redis]
  celery:
    build:
      context: .
      dockerfile: ./celery/Dockerfile
    image: zydhanlinnar11/dmoj-celery
    restart: unless-stopped
    volumes:
      - datacache:/datacache/
      - ./logs/:/logs/
      - ./repo/:/site/
    working_dir: /site/
    env_file: [environment/mysql.env, environment/site.env]
    networks: [site, db]
    depends_on: [base, db, redis]
  bridged:
    build:
      context: .
      dockerfile: ./bridged/Dockerfile
    image: zydhanlinnar11/dmoj-bridged
    restart: unless-stopped
    volumes:
      - ./logs/:/logs/
      - ./problems/:/problems/
      - ./repo/:/site/
    working_dir: /site/
    env_file: [environment/mysql.env, environment/site.env]
    networks: [site, nginx, db]
    ports:
      - 9999:9999
    depends_on: [base, db, redis]
  nginx:
    container_name: dmoj_nginx
    image: nginx:alpine
    restart: always
    ports:
      - 8080:80
    volumes:
      - assets:/assets/
      - pdf:/pdf/
      - ./logs/:/logs/
      - ./media/:/media/
      - ./nginx/conf.d/:/etc/nginx/conf.d/
    networks: [nginx]
    depends_on: [site]

networks:
  site:
  db:
  nginx:
volumes:
  assets:
  pdf:
  datacache:
